package fr.croixrouge.storage.model.quantifier;

public class VolumeQuantifier extends Quantifier {
    public VolumeQuantifier(float quantity, VolumeUnit type) {
        super(quantity, type);
    }
}
